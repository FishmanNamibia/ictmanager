# I-ICTMS Frontend — copy to .env.local
# Backend API base URL (must end without a trailing slash)
# If not set, falls back to http://<hostname>:3001/api at runtime
NEXT_PUBLIC_API_URL=http://localhost:3001/api
